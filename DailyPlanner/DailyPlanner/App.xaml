<?xml version="1.0" encoding="utf-8" ?>
<prism:PrismApplication xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             x:Class="DailyPlanner.App">
    
    <prism:PrismApplication.Resources>
        <ResourceDictionary>
            <Style x:Key="FrameStyle" TargetType="Frame">
                <Setter Property="Margin" Value="5" />
                <Style.Triggers>
                    <DataTrigger TargetType="Frame" Binding="{Binding IsSelected}" Value="True" >
                        <Setter Property="BackgroundColor" Value="#d21c26" />
                    </DataTrigger>
                    <DataTrigger TargetType="Frame" Binding="{Binding IsSelected}" Value="False" >
                        <Setter Property="BackgroundColor" Value="White" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
            <Style x:Key="SelectionBorderStyle" TargetType="BoxView">
                <Setter Property="HeightRequest" Value="1" />
                <Style.Triggers>
                    <DataTrigger TargetType="BoxView" Binding="{Binding IsSelectable}" Value="True" >
                        <Setter Property="BackgroundColor" Value="#d21c26" />
                    </DataTrigger>
                    <DataTrigger TargetType="BoxView" Binding="{Binding IsSelectable}" Value="False" >
                        <Setter Property="BackgroundColor" Value="White" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </ResourceDictionary>
    </prism:PrismApplication.Resources>
    <!-- Application resource dictionary -->

</prism:PrismApplication>